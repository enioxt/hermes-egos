# Upstream Hermes — Sync Report 2026-09-11

**Prioridade:** 🔴 SECURITY
**Commits novos:** 559

| Categoria | Quantidade | Ação recomendada |
|-----------|------------|-----------------|
| 🚨 BREAKING | 0 | Planejar rollout — avisar clientes 7d antes |
| 🔴 SECURITY | 34 | Merge em <48h obrigatório |
| 🟢 BUGFIX | 320 | Merge no próximo sprint semanal |
| 🟡 FEATURE | 44 | Avaliar utilidade para clientes Central EGOS |

## Commits

```
a2413495a fix(state): fts_trigram_session_sql qualifies model_config under the json_valid guard
46afbfec1 fix(state): route the remaining model_config marker reads through _sql_json_extract
c4a8572d6 test(state): pin corrupt-row robustness — one bad timestamp/oversized IN-list never kills the command
25670cd9e fix(state): batch large session cleanup queries below SQLite's variable limit
acfda3759 fix(sessions): chunk IN-list ids in bulk delete to avoid 'too many SQL variables'
496eb13bd fix(state): one corrupt timestamp row no longer kills sessions list, export or insights
df1388e77 test(tools): extend the delegation fd-leak injection to the schema replay
a6d65cdd0 fix(state): single durable-shape authority for async_delegations
0bb27d37c fix(state): declare origin_session_id in the canonical async_delegations schema
a239c4f81 fix(state): tolerate malformed session marker JSON
78f85112d perf(state): batch message hydration in export_all
16e4496d9 fix(agent): corrupt-session recovery guidance names the session's own state.db
df0eed4f6 fix(session_search): a bare session id never reads another profile's state.db
5a720bbb1 test(tui): launch-handle pin test opens a real registry SessionDB
342705135 test(tui): cover launch SessionDB binding under profile override windows
3c3ca061a fix(desktop): pin launch SessionDB handle to the launch home
3a350508f fix(cli): wait for SessionDB teardown when deleting a profile
f1ee8e0f3 fix(cli): release SessionDB handles when deleting a profile
d956e05e5 chore: contributor mappings for the statedb-wal salvage (gaoanze888 work email, nikkoxgonzales noreply)
e00a21cd3 fix(state): retry a transient SQLITE_IOERR on the pooled read path before surfacing it
295edf255 fix(state): gate the lock-free read pool on a confirmed WAL header, not the assumed mode
9f9ec647f fix(gateway): re-check the live store before broadcasting the state.db warning
045704ce0 test(state): port the #107411 dentry tests to the shared WAL-generation harness
4d9ec9be5 fix(db): survive DELETE fallback failure in WAL setup
031d3760d fix(state): distinguish retired WAL recovery guidance
3a3ec6eea fix(state): serialize replaced/generation probe with close() in _execute_write
d6b036b72 fix(state): compare fd identity against the watched path, not st_nlink
84a3c4de7 fix(state): require nlink==0 before treating a /proc fd as an unlinked WAL sidecar
7dec81568 test(desktop): trim salvaged pool tests to invariants
19cff347a fix(desktop): wait for an evicted backend to release its slot
919dea902 fix(desktop): await LRU teardown before queued wake
a863bbcc2 fix(desktop): make pool slot timeouts actionable
d27180ba7 fix(desktop): pin primary backend routing to its booted profile
d1bd7b00d fix(desktop): dispatch probe falls back to /api/status on pre-/api/health remotes
ee8257d60 fix(desktop): probe /api/health on pooled SSH dispatch, not /api/status
8ab08968f fix(desktop): give pooled remote dispatch probes the cold-start liveness budget
902eccae3 test(state): trim holder-scope suite to the two behaviour contracts
085623a11 fix(state): scope uninspectable-holder fallback to the instance's own HERMES_HOME
d41e90659 test(hosted-rooms): pin the coordination store off the master state.db; label sidecars
cfeccbdf3 fix(hosted-rooms): local_profiles skips the profile-delete tombstone dir
c9b71ebaf fix(dashboard): startup schema reconcile opens state.db read-only first
b02f3aa00 fix(doctor): refuse the WAL checkpoint while a live writer holds state.db (#103339)
d294a655f fix(cron): serialize lifecycle script reads with sqlite locks
4e743b06a chore: contributor mapping for RikETS
0e422e0ec fix(gateway): isolate hosted-room state in shared-state.db — profile gateways must not write master state.db
52fb4e087 fix(desktop): read Bot Mode avatars over the active socket, not one dial per bot
e4080b381 fix(desktop): roster avatar sync no longer dials a backend per registered profile at launch
ff59fcd71 fix(state): skip the display-trigger drop+recreate when the triggers already match
c4c7e102a refactor(state): tighten generation-stamp comment to the why
b8d7977f0 test(state): trim #101881 tests to two invariants (zero writes on settled open; open not blocked by sibling write lock)
```

## Decisão necessária

- [ ] Revisar commits acima
- [ ] Decidir: MERGE | ADIAR | IGNORAR por categoria
- [ ] Executar sync se necessário:

```bash
git fetch upstream
git rebase upstream/main
# Resolver conflitos (ver EGOS-FORK-GUIDE.md)
git push origin main
```

---
*Gerado automaticamente — 2026-09-11 13:28 UTC*
