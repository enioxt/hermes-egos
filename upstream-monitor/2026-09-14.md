# Upstream Hermes — Sync Report 2026-09-14

**Prioridade:** 🔴 SECURITY
**Commits novos:** 308

| Categoria | Quantidade | Ação recomendada |
|-----------|------------|-----------------|
| 🚨 BREAKING | 0 | Planejar rollout — avisar clientes 7d antes |
| 🔴 SECURITY | 8 | Merge em <48h obrigatório |
| 🟢 BUGFIX | 189 | Merge no próximo sprint semanal |
| 🟡 FEATURE | 20 | Avaliar utilidade para clientes Central EGOS |

## Commits

```
9b419a2d3 fix(state): holder scan resolves symlinked homes; doctor's ro URI escapes reserved chars
12173db5b fix(state): second-process maintenance on state.db refuses ANY foreign holder
4da41c92b test(tools): count each tool receipt once and pin the quiet-notify follow-up
6f6c74101 test(cli): bind the shared linger budget, the timeout stop, and the finalize skip
5d4231997 fix(cli): quiet -Q drain must use the async_delegation delivery ledger
d328adf61 fix(bot-mode): strip canonical session env names, keep HERMES_SESSION_* knobs
66fa63820 fix(cli): flag the quiet -Q linger as consumed only after the loop runs
1f3407aa0 fix(cli): sync session id after quiet -Q follow-up turns
30b02bba1 refactor(cli): quiet -Q session binding as a context manager
d503526b8 fix(cli): quiet -Q notify loop must not drop owned async_delegation events
c429081bc fix(cli): one shared linger budget for the quiet -Q notify-resume loop
314f3d135 test(bot-mode): pin nested quiet-notify ownership and delivery env isolation
9a6c7a931 fix(bot-mode): resume nested one-shot DMs on the recipient's session
5c26cf64b chore: author map for xxxigm (PR #110866 bot-mode nested one-shot DM resume)
efca6efdc refactor(hermes_state): one canonical_sqlite_path
afeaabf62 refactor(agent_init): reuse anthropic_adapter._TOOL_STREAMING_BETA
e6e4213db fix(lifecycle-ledger): pre-stamp sentinels still tell an owner from a PID reuser
40087fba7 fix(lifecycle-ledger): a --replace handover is no longer reported as an unclean death
8c3a35b69 fix(gateway-windows): bind the attestation to process birth, not the ledger claim
faf6e6889 fix(gateway-windows): bind the start attestation to each PID's process create time
8c13452b4 fix(gateway-windows): a start attestation past the horizon is no authority for a cold-start
060c0cd0f fix(update): authorize the Windows cold-start from the resume token's attestation generation
ccc335cdb fix(update): consume the start attestation only after the cold-start is ready
aec8a5861 fix(gateway-windows): attestation PID list accepts exact positive ints only
d4063e626 test: SessionDB doubles accept the read_only kwarg / registry acquire the CLI now uses
1ce2d2523 test: achievements scan attaches read-only; read-only handles never trip the duplicate-writer warning
939a2f64b fix: long-lived processes stop minting duplicate state.db writer handles
7064c28c9 refactor(dashboard): startup reconcile reuses the routers' own-store opener
b0e0067b6 chore: map contributor emails for kokhlo and Rroven
ca6b189a7 fix(codex): both transaction locks wait out the endpoint; adopt only a complete pair
e117e792b fix(codex): run the refresh inside the source store's transaction so shared-root peers adopt, not replay
a19160bbf refactor(agent): one outbound-kwargs sanitizer seam for the main loop and the summary
8c6cb6f99 refactor(agent): drop the `Optional` import orphaned by the LM Studio helper removal
f4442e44b refactor(agent): keep `_summary_text`; the tool-call log is the only new behaviour
53cfca976 fix(agent): run the outbound payload sanitizers on the summary request
488f2fc86 refactor(agent): drop the orphaned hand-rolled summary kwargs builder
419a05042 fix(agent): preserve tool cache in iteration summary
2edc1249f chore: map ywatanabe@scitex.ai -> @ywatanabe1989 (PR #110480 salvage)
9b199246e fix(desktop): cancelAndWait resolves only once the composed drain settles
2aca48b41 fix(desktop): same-scope SSH drains compose instead of replacing the barrier
4748caff7 fix(gateway): explicit tool_progress new/all keeps text progress in un-cardable Slack chats
e9c037f65 docs(slack): clarify progress resolution and fallback lifetime
a4e2a82a6 fix(gateway): preflight task-card destination before transport fallback
dab7eebf4 fix(gateway): resolve progress mode and intent from the same source
bc125d59d fix(gateway): null tool_progress inherits; name the task-card suppression latch
3412490ad fix(gateway): no text tool progress when a Slack chat cannot host a task card
ed25a4091 fix(gateway): explicit tool_progress off disables Slack task cards
62e5f4665 test(streaming): one Anthropic event-stream fake for the three parse-error tests
32a0d2d2e refactor(agent): one home for the stream parse-error markers
982e50426 fix(anthropic): partial tool names are reset per stream attempt
```

## Decisão necessária

- [ ] Revisar commits acima
- [ ] Decidir: MERGE | ADIAR | IGNORAR por categoria
- [ ] Executar sync se necessário:

```bash
git fetch upstream
git rebase upstream/main
# Resolver conflitos (ver EGOS-FORK-GUIDE.md)
git push origin main
```

---
*Gerado automaticamente — 2026-09-14 15:51 UTC*
