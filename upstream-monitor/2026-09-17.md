# Upstream Hermes — Sync Report 2026-09-17

**Prioridade:** 🔴 SECURITY
**Commits novos:** 411

| Categoria | Quantidade | Ação recomendada |
|-----------|------------|-----------------|
| 🚨 BREAKING | 0 | Planejar rollout — avisar clientes 7d antes |
| 🔴 SECURITY | 6 | Merge em <48h obrigatório |
| 🟢 BUGFIX | 257 | Merge no próximo sprint semanal |
| 🟡 FEATURE | 28 | Avaliar utilidade para clientes Central EGOS |

## Commits

```
c712f06dc docs(bot-mode): name the real create control, scope the isolation claim
48566fd94 fix(docs): clarify bot credential isolation
c96987a51 chore(contributors): map KoNit-K for the bot-mode credential docs salvage (#114001)
228022ef5 test(cli): file-url tests stay host-neutral and cover the non-image case
1e32d86ee refactor(cli): gate the macOS file-url read on one clipboard-info listing
045778e3f fix(cli): attach macOS clipboard images copied as files
06ae26bf7 test(cli): cover macOS clipboard image file-url paste
98f758ae7 fix(upstage): default new Solar models to the 512K context window
251e5f7c0 refactor(tui_gateway): one lease-metadata constructor, takeover selector/effector split, mark clears on re-own
8039bb1e6 fix(tui_gateway): lease takeover stays within the profile and spares the new owner's row
3ca367ad8 fix(tui_gateway): a chat restored under a new runtime takes over its own detached lease
f5d192611 test(agent): pin the eviction policy arithmetic directly
723e3fb9a docs(computer-use): screenshot eviction fires at the provider limit, not keep-3
fa5da7cf1 perf(agent): size outbound image payloads by data length, not by re-serializing them
30ca0e107 fix(agent): quantize eviction by the fit window so heavy tool results hold the frontier
5dcfae44b refactor(agent): one image-eviction policy for both send-path passes
09d68f8a6 fix(agent): a batch retire that would blind the model stops at the keep-newest floor
593a2f107 fix(anthropic): the screenshot-eviction floor is satisfiability-aware on the wire pass too
b150dfa87 fix(agent): make the keep-newest floor satisfiability-aware
406602bec fix(anthropic): walk sibling tool_result blocks newest-first when evicting screenshots
5768f78e4 fix(agent): count image BLOCKS and reserve user uploads against the limit
c37753c42 fix(agent): round the eviction overshoot up to whole batches so the image limit actually holds
66a281c33 fix(agent): evict images at the provider limit, not on a keep-newest count
bbaf7af5c fix(desktop): await page-owned promises before Electron IPC
4146fcbd7 test(desktop): drop the source-text cpSync/rmSync guard
afb9243f9 fix(desktop): widen libuv-only staging to get-windows and the rollback .bak wipe
8317fc8f4 fix(desktop): harden before-pack cleanup against the same non-ASCII rmSync no-op
2d88b9b4d fix(desktop): stage node-pty without fs.cpSync/fs.rmSync (non-ASCII Windows paths)
3dcf0d49a fix(install): let Rolldown name the missing binding; repair on every OS
f67cad172 test(desktop): register Rolldown repair tests with Vitest
ae9f42acc fix(install): repair missing Rolldown bindings
58a41bcc2 chore: map contributor email for neil771
9f077100c fix(desktop): enable New Group Chat with one local bot plus remote-connection bots
00cb8895d fix(gateway): pre-isolation room import copies per room, reads pre-actor layouts, warns once
d62036acb test(desktop): e2e — pre-split hosted rooms stay reachable from the Desktop backend
5e9fe1016 fix(gateway): import pre-isolation hosted_room* rows into shared-state.db (#109775)
a08dee94c fix: read plugin-home config through the loader; register update export
6ac3ff886 feat(plugins validate): fail trees with nothing to load; check dependency declarations
96e8a2322 feat(plugins): install declared Python dependencies and re-apply them after hermes update
e3507ef2c feat(lazy_deps): install_specs takes constraints and dry_run
4a61b030b fix(nous): heal a signed-in account's stale welcome-host route once before failing the turn
bec892459 fix(update): hand-off carries sibling snapshots + Windows pause token on both paths; child pinned to the parent's home
32d958bcd docs(update): describe the post-swap hand-off; add its live A/B harness
ae71d85bb fix(update): startup hint no longer blames an update that did restart the fleet
94ced1a2b fix(update): finish `hermes update` in an interpreter born on the pulled code
a24b3ecc5 test(nous): one case per changed branch in the identity matrix; cover the escaped-exception surface
564687b11 fix(nous): classify the desktop's escaped-exception card with the request credential
149c2d323 test(nous): give the outage-copy negatives their anonymous arm; trim the identity matrix
f7da523ae refactor(nous): one is_anonymous_agent seam for the five identity reads
86d393d78 perf(aux): bound the anonymous Nous cooldown marker instead of skipping it
```

## Decisão necessária

- [ ] Revisar commits acima
- [ ] Decidir: MERGE | ADIAR | IGNORAR por categoria
- [ ] Executar sync se necessário:

```bash
git fetch upstream
git rebase upstream/main
# Resolver conflitos (ver EGOS-FORK-GUIDE.md)
git push origin main
```

---
*Gerado automaticamente — 2026-09-17 14:13 UTC*
