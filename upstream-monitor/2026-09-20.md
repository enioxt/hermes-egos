# Upstream Hermes — Sync Report 2026-09-20

**Prioridade:** 🔴 SECURITY
**Commits novos:** 200

| Categoria | Quantidade | Ação recomendada |
|-----------|------------|-----------------|
| 🚨 BREAKING | 0 | Planejar rollout — avisar clientes 7d antes |
| 🔴 SECURITY | 2 | Merge em <48h obrigatório |
| 🟢 BUGFIX | 94 | Merge no próximo sprint semanal |
| 🟡 FEATURE | 2 | Avaliar utilidade para clientes Central EGOS |

## Commits

```
2ed6387d8 test(gateway): status readers flush the queued write before reading the file
644870095 perf(gateway): two deep copies per publish, not four
f78148c7b refactor(gateway): drop the redundant writer lock
97b299153 refactor(gateway): publish/write forward **fields to the shared preparer
9d5793702 refactor(gateway): runtime-status flush relays through to_thread
22169ff8a fix(gateway): runtime-status transitions are reported when queued, not only when persisted
7e15a9aaf fix(gateway): shutdown flush budget applies to signal-driven stops only
b8a21e0ed test(gateway): bounded-wait test waits for the writer to start
489df88d6 fix(gateway): runtime-status writer merges over out-of-process stamps
d27ee657b fix(gateway): shutdown flush cannot outlive the launchd exit budget
15c09ebd3 refactor(gateway): public kwargs and event-driven flush wait
c5b32ff8f refactor(gateway): start the runtime-status flush once at startup
d5479495b fix(gateway): persist runtime status off event loop
5ae482031 chore: map kirkins for attribution (#116289)
76b3c7ec1 fix(gateway): keep the os.getuid gate the Windows-footgun lint requires
184c70f5f refactor(gateway): one effective watchdog leash helper for all three stop-path clamps
2e63bf9ca refactor(gateway): launchd loader logs the label it resolves
23bd1ee63 fix(gateway): launchd identity requires darwin, like control_socket
3dc170e58 refactor(gateway): launchd drain clamp coerces through _seconds
84c27f569 test(gateway): SIGTERM handler marks the stop as signal-driven
33e950338 refactor(gateway): launchd exit-timeout loader lives with the other timeout loaders
5c98c1db2 refactor(gateway): watchdog leash reuses the launchd drain clamp
d5f3d8d8d fix(gateway): only ai.hermes launchd jobs get the ExitTimeOut drain cap
8010dbfd7 test(gateway): trim launchd drain-cap tests to the two invariants
05f429006 chore: map Kyzcreig for attribution (#116493)
dc0f03ace fix(gateway): thread watchdog leash also fits launchd's exit budget
25507c4c0 refactor(gateway): drop launchd drain-cap scaffolding that production never calls
815594ee2 fix(gateway): resolve os.getuid via getattr in launchd probe (Windows footgun lint)
aa0289f30 fix(gateway): cap signal-driven stop drain to launchd's live ExitTimeOut
639823919 fix(mcp_startup): take the deferred slot under the lock so two racing first clients fire once
558356604 refactor(dashboard): fire the armed discovery off-loop; flat branch; behaviour-shaped test
b47abc36f fix(dashboard): standalone dashboard starts MCP discovery on first /api/ws client, not at boot
0c1bd3bb8 test(skills): restore the good fetcher before the clean-fetch assert
4f9e15f1d fix(skills): a bundle with a failed blob fetch records no revision; tree misses are cached; the probe is guarded
075ed5549 perf(skills): skip bundle downloads when the GitHub tree revision is unchanged
297b3cefa fix(gateway): say so when an explicit text-batch delay is clamped to the ceiling
de083436b refactor(gateway): drop Telegram's shadowing _coerce_float_extra; module-level math import
530fdb586 refactor(gateway): one text-batch cadence and one extra-float parser on BasePlatformAdapter
2fb3f9121 test(gateway): WhatsApp/Weixin text-batch defaults stay within Telegram cadence
130b596f6 fix(whatsapp,weixin): text-batch delays default to Telegram cadence
52d203d04 fix(agent): read the trim flag with a default — bare test stubs reach _execute_tool_calls without _set_defaults
e74a84f0b refactor(agent): declare the trim flag in _CONTROL_STATE; trim only on a completed batch
05e09aee6 fix(agent): trim after the tool batch unwinds, not inside the commit that still holds the result
f32f65178 perf(tool_executor): trim memory after publishing a >=1 MB tool result
f458cba71 test(matrix): drop the three adapter knobs the pending-invite tests never read
18f170613 refactor(matrix): drop dead is_direct guard and unreachable sender warning
b98dd9914 docs(matrix): reconcile gate comment states the real premise
f49fa66eb test(matrix): keep two invariant tests for the pending-invite gate
439f2b1cf fix(matrix): apply the inviter allowlist to reconciled pending invites
0ce3e7b12 fix(matrix): record reconciled direct invites in m.direct
```

## Decisão necessária

- [ ] Revisar commits acima
- [ ] Decidir: MERGE | ADIAR | IGNORAR por categoria
- [ ] Executar sync se necessário:

```bash
git fetch upstream
git rebase upstream/main
# Resolver conflitos (ver EGOS-FORK-GUIDE.md)
git push origin main
```

---
*Gerado automaticamente — 2026-09-20 13:32 UTC*
