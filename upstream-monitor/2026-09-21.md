# Upstream Hermes — Sync Report 2026-09-21

**Prioridade:** 🔴 SECURITY
**Commits novos:** 200

| Categoria | Quantidade | Ação recomendada |
|-----------|------------|-----------------|
| 🚨 BREAKING | 0 | Planejar rollout — avisar clientes 7d antes |
| 🔴 SECURITY | 5 | Merge em <48h obrigatório |
| 🟢 BUGFIX | 126 | Merge no próximo sprint semanal |
| 🟡 FEATURE | 14 | Avaliar utilidade para clientes Central EGOS |

## Commits

```
bb359ec5c fix(gateway): log the converge hint on a standalone owner; put a refusal in the profile's own log
d39ce8f45 refactor(gateway): build the rescanned owner once; fix docstrings the new outcome contradicted
804cc3c02 fix(gateway): a standalone owner is the per-profile topology, not a refusal
75b083e93 fix(ci): eslint ignores *.generated.ts; restore the contract file's header
bc655bfb4 fmt(js): `npm run fix` on merge (#118250)
5bb314fa0 docs: link the Skills and Plugins hubs from the doc sidebar
782886f87 fix(desktop): bound the Chromium log while the shell is running, not only at launch
9a2179445 fix(desktop): optional Linux crash diagnostics must not be fatal or unbounded
5c57bfdca refactor(desktop): a shared, path-parameterized log rotation planner
3be255eca test(update): the host obligation is host-scoped state, so each test must discharge it
953b6f6f0 fix(update): never disarm the host restart obligation, and never discharge unknown terms
061195fac FLEET: one host-scoped update-restart obligation, one restart per host
8f7b3b48f fix(desktop): close the Linux notification bus on will-quit
a327f1ec1 test(desktop): cover both notification transports from any host
c93b15724 fix(desktop): no notification cooldown after a benign daemon swap on Linux
f09af639a refactor(desktop): drop unreachable fences in the Linux notification transport
ab4455d96 fix(desktop): release naturally closed Linux notifications
326ce363a fix(desktop): prevent Linux native notification freezes
274bc7b8f docs(desktop): where the Linux Chromium log and minidumps live
00e08cec3 fix(desktop): a Linux SIGTRAP leaves its FATAL line and a minidump behind
ea0c2b820 fix(host-topology): a createTime-null record is not a live gateway, and the restart we print must run
f66089174 fix(host-topology): only a PROVEN-live host record names the host gateway
a1398a6f6 fix: doctor, cron status, claw and gateway status report the real host topology
a10620a66 fix(gateway): a record alone never means "attach", and --replace/--force work
a20a88398 gateway: lifecycle verbs mean "the one host multiplexer"
7b660e66e fix(homeassistant): detect the supervised launch through is_supervised_gateway_launch
eda2432cc fix(gateway): scope the Local Network hint and keep the osascript wrapper out of gateway process scans
5d63d3e5c fix(gateway): launchd gateway reaches the local network on macOS (#71206, #57812)
61938453c test(homeassistant): errno 65 under launchd names the Local Network remedy (#71206)
1d871110e fix(homeassistant): name the macOS Local Network block behind errno 65 under launchd (#71206)
6668ac14a fix(desktop): count the answer row in a page-boundary fold
3f985c84c fix(desktop): count released transcript rows in backend rows
7c799c26c fix(desktop): rewind the older-page offset in the backend's own units
71a0ce913 refactor(desktop): share the tail-entry lookup, and plan nothing when nothing is released
14b432744 fix(desktop): the store releases paged-through history instead of holding it for the window's lifetime (#77311)
db1f3f456 fix(computer-use): doctor names the stale TCC row on the health_report path too
42ae8b441 fix(computer-use): name the stale TCC row when CuaDriver shows ON but is denied
080907e3b chore(auth): drop the unused tool:invoke OAuth scope from the step-up request (#118036)
bfcd20990 docs(multiplexing): record the config escape hatch and the .env-over-frozen-env precedence
af380f66a fix(mcp): divide the shutdown budget across profile passes and stop poisoning the teardown context
bc0a42fd9 fix(kanban): scrub the dispatcher's credentials from another profile's worker on any host
d80aaf1c0 fix(serve): honour multiplex_profiles: false, freeze the launch env last, be loud on failure
cbeedffc9 fix(tui_gateway): never run an external secret command inside the exit-flush budget
4aa9baf13 fix(multiplex): a profile whose home does not resolve must not borrow the launch profile's secrets
c718267ef fix(multiplex): close the profile-scope holes the scope machinery misses
8b71dc31e fix(state): VACUUM admission counts live holders only; argv proves the home, not the install
a2c0af430 fix(state): per-profile store housekeeping under one multiplexed process
9ecd22e5d fix(cron): release a profile's in-flight claim under the key it registered with
cb647a018 fix(cron): one host ticker owns every profile's cron, per profile
5289a0058 docs: unescape a quote in the HostLockOutcome docstring
```

## Decisão necessária

- [ ] Revisar commits acima
- [ ] Decidir: MERGE | ADIAR | IGNORAR por categoria
- [ ] Executar sync se necessário:

```bash
git fetch upstream
git rebase upstream/main
# Resolver conflitos (ver EGOS-FORK-GUIDE.md)
git push origin main
```

---
*Gerado automaticamente — 2026-09-21 15:49 UTC*
